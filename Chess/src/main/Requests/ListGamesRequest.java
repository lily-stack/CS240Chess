package Requests;

import Models.User;

/**
 * Represents a request from a user to list all games
 */
public class ListGamesRequest {
    /**
     * creates an instance of a request to list all games
     */
    public ListGamesRequest() {}
}
